<Window x:Class="WindowsMLSampleForWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsMLSampleForWPF"
        mc:Ignorable="d"
        Title="Windows ML WPF Sample - SqueezeNet Image Classification" Height="650" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Controls Row -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- First row: Image selection and inference -->
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Name="SelectImageButton" Click="SelectImageButton_Click" Content="Select Image" Grid.Column="0" Margin="0,0,10,0" Padding="10,5" HorizontalAlignment="Left"/>
                <TextBox Name="ImagePathTextBox" Grid.Column="1" Margin="0,0,10,0" VerticalAlignment="Center" IsReadOnly="True" Text="No image selected"/>
                <Button Name="RunInferenceButton" Content="Run Inference" Grid.Column="2" Padding="10,5" HorizontalAlignment="Right" Click="RunInferenceButton_Click" IsEnabled="False"/>
            </Grid>
            
            <!-- Second row: Provider download checkbox -->
            <Grid Grid.Row="1">
                <CheckBox Name="AllowProviderDownloadCheckBox" Content="Allow execution provider downloads" HorizontalAlignment="Left" IsChecked="False" Margin="0,5,0,0"/>
            </Grid>
        </Grid>
        
        <!-- Image Display Row -->
        <GroupBox Header="Selected Image" Grid.Row="1" Margin="0,0,0,10" MinHeight="250">
            <Border BorderBrush="Gray" BorderThickness="1" Background="LightGray">
                <Image Name="SelectedImage" Stretch="Uniform" MaxHeight="200"/>
            </Border>
        </GroupBox>
        
        <!-- Results Row -->
        <GroupBox Header="Classification Results" Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Name="ResultsTextBox" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" 
                         IsReadOnly="True" 
                         FontFamily="Consolas"
                         Background="White"
                         BorderThickness="0"
                         Text="Select an image and click 'Run Inference' to see classification results." />
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>
