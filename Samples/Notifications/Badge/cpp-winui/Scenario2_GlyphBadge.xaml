<!-- Copyright (c) Microsoft Corporation.
     Licensed under the MIT License. -->
<Page
    x:Class="cpp_winui.Scenario2_GlyphBadge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:cpp_winui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel Spacing="10" Margin="10,10,10,10">
                <TextBlock Text="Description:" Style="{StaticResource SampleHeaderTextStyle}"/>
                <TextBlock Style="{StaticResource ScenarioDescriptionTextStyle}" TextWrapping="Wrap">
                    This sample demonstrates how to display a Glyph badge notification for an app.
                </TextBlock>
                <Button x:Name="activityBadge" Click="activityBadge_Click" Margin="0 0 0 10">Activity</Button>
                <Button x:Name="alarmBadge" Click="alarmBadge_Click" Margin="0 0 0 10">Alarm</Button>
                <Button x:Name="alertBadge" Click="alertBadge_Click" Margin="0 0 0 10">Alert</Button>
                <Button x:Name="attentionBadge" Click="attentionBadge_Click" Margin="0 0 0 10">Attention</Button>
                <Button x:Name="availableBadge" Click="availableBadge_Click" Margin="0 0 0 10">Available</Button>
                <Button x:Name="awayBadge" Click="awayBadge_Click" Margin="0 0 0 10">AwayBadge</Button>
                <Button x:Name="busyBadge" Click="busyBadge_Click" Margin="0 0 0 10">Busy</Button>
                <Button x:Name="errorBadge" Click="errorBadge_Click" Margin="0 0 0 10">Error</Button>
                <Button x:Name="newMessageBadge" Click="newMessageBadge_Click" Margin="0 0 0 10">New Message</Button>
                <Button x:Name="pausedBadge" Click="pausedBadge_Click" Margin="0 0 0 10">Paused</Button>
                <Button x:Name="playingBadge" Click="playingBadge_Click" Margin="0 0 0 10">Playing</Button>
                <Button x:Name="unavailableBadge" Click="unavailableBadge_Click" Margin="0 0 0 10">Unavailable</Button>
                <Button x:Name="clearBadge" Click="clearBadge_Click" Margin="0 0 0 10">Clear badge</Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
