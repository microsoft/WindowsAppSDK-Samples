<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright (c) Microsoft Corporation.
     Licensed under the MIT License. -->
<Page
    x:Class="SDKTemplate.Scenario1_Launch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKTemplate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="16,24,16,16">
        <StackPanel Spacing="8">
            <TextBlock Text="Description" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock TextWrapping="Wrap">
                The touch keyboard displays
                when touch or pen is used to set focus on a XAML text control
                (such as TextBox, RichTextBox, and PasswordBox)
                or a control derived from a XAML text control,
                subject to the note below.
            </TextBlock>

            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,16,0,0">
                <TextBox
                    AutomationProperties.AutomationId="StandardTextBox"
                    Header="TextBox control"
                    PlaceholderText="The keyboard displays when you tap here." />
                <local:CustomTextBox
                    AutomationProperties.AutomationId="CustomTextBox"
                    Header="Custom control derived from TextBox"
                    PlaceholderText="The keyboard displays when you tap here." />
                <StackPanel Spacing="8" VerticalAlignment="Bottom">
                    <TextBlock>The keyboard hides when you tap the button.</TextBlock>
                    <Button
                        AutomationProperties.AutomationId="DismissButton"
                        Content="Dismiss keyboard" />
                </StackPanel>
            </StackPanel>

            <TextBlock
                Text="Note"
                Style="{StaticResource SubtitleTextBlockStyle}"
                Margin="0,32,0,0" />
            <TextBlock TextWrapping="Wrap">
                The touch keyboard displays automatically when
                <LineBreak /><Bold>1.</Bold> A hardware keyboard is not active, and
                <LineBreak /><Bold>2.</Bold> Either of the following is true:
            </TextBlock>
            <TextBlock TextWrapping="Wrap" Margin="20,0,0,0">
                <Bold>2a.</Bold> Tablet mode is turned on, or
                <LineBreak /><Bold>2b.</Bold> "Show the touch keyboard when not in tablet mode
                and there's no keyboard attached" is enabled in
                <Bold>Settings &#8594; Devices &#8594; Typing</Bold>.
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</Page>
