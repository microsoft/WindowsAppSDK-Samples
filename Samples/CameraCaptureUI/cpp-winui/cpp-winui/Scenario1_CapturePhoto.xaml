<!-- Copyright (c) Microsoft Corporation.
     Licensed under the MIT License. -->
<Page
    x:Class="cpp_winui.Scenario1_CapturePhoto"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:cpp_winui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="*,Auto">
        <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel Spacing="20" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Top">

                <!-- Header -->
                <TextBlock 
                Text="Capture a Photo with CameraCaptureUI"
                Style="{StaticResource TitleTextBlockStyle}" 
                HorizontalAlignment="Center" 
                FontSize="28" 
                FontWeight="Bold"/>

                <!-- Description -->
                <TextBlock 
                Text="This sample demonstrates how to capture a photo using the CaptureFileAsync API in CameraCaptureUI. Click the button below to take a picture."
                Style="{StaticResource BodyTextBlockStyle}" 
                TextWrapping="Wrap" 
                HorizontalAlignment="Center" 
                Margin="0,10" 
                FontSize="16" 
                Foreground="#6D6D6D"/>

                <!-- Capture Photo Button -->
                <Button
                Content="Capture Photo"
                Click="CapturePhoto_Click"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground ="Red"
                FontSize="16"
                Width="200"
                Height="50"
                BorderThickness="2"
                CornerRadius="25"/>

                <!-- Captured Image Display Area -->
                <Image 
                x:Name="CapturedImage"
                Width="300"
                Height="300"
                Stretch="Uniform"
                Margin="0,20"
                HorizontalAlignment="Center" />

                <!-- Clear Messages Button -->
                <Button
                Content="Clear Message"
                Click="ClearMessage_Click"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="Transparent"
                Foreground="#FF6C6C6C"
                FontSize="14"
                Width="150"
                BorderBrush="Transparent"
                BorderThickness="0"
                Margin="0,10"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
