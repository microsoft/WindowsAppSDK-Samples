<!-- Copyright (c) Microsoft Corporation.
     Licensed under the MIT License. -->
<Page
    x:Class="ConditionalXamlPredicate.Scenario1_LoadConditionalControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ConditionalXamlPredicate"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:featureABCEnabled="http://schemas.microsoft.com/winfx/2006/xaml/presentation?local:MyCustomPredicate(FeatureABC,true)"
	xmlns:featureABCDisabled="http://schemas.microsoft.com/winfx/2006/xaml/presentation?local:MyCustomPredicate(FeatureABC,false)"
	xmlns:featureXYZEnabled="http://schemas.microsoft.com/winfx/2006/xaml/presentation?local:MyCustomPredicate(FeatureXYZ,true)"
	xmlns:featureXYZDisabled="http://schemas.microsoft.com/winfx/2006/xaml/presentation?local:MyCustomPredicate(FeatureXYZ,false)"
    mc:Ignorable="d">

    <Grid RowDefinitions="*,Auto">
        <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel Spacing="10" Margin="10,10,10,10">
                <TextBlock Text="Description:" Style="{StaticResource SampleHeaderTextStyle}"/>
                <TextBlock Style="{StaticResource ScenarioDescriptionTextStyle}" TextWrapping="Wrap">
                    This scenario demonstrates conditional XAML loading using custom predicates.
                    Controls below are conditionally loaded based on the FeatureABC flag.
                </TextBlock>
                
                <!-- Controls visible when FeatureABC is ENABLED -->
				<featureABCEnabled:TextBlock Text="FeatureABC is ENABLED" 
                    Foreground="Green" FontWeight="Bold"/>
				<featureABCEnabled:Button Content="ABC Feature Action" />
                
                <!-- Controls visible when FeatureABC is DISABLED -->
				<featureABCDisabled:TextBlock Text="FeatureABC is DISABLED" 
                    Foreground="Red" FontWeight="Bold"/>
				<featureABCDisabled:Button Content="ABC Fallback Action" />

                <!-- Controls visible when FeatureXYZ is ENABLED -->
				<featureXYZEnabled:TextBlock Text="FeatureXYZ is ENABLED" 
                    Foreground="Blue" FontWeight="Bold"/>
				<featureXYZEnabled:Button Content="XYZ Feature Action" />
                
                <!-- Controls visible when FeatureXYZ is DISABLED -->
				<featureXYZDisabled:TextBlock Text="FeatureXYZ is DISABLED" 
                    Foreground="Gray" FontWeight="Bold"/>
				<featureXYZDisabled:Button Content="XYZ Fallback Action" />

                <!-- Nested example: Parent always visible, children conditional -->
                <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,10,0,0">
                    <StackPanel Spacing="5">
                        <TextBlock Text="Nested Controls Demo" FontWeight="Bold"/>
                        
                        <!-- Parent control under FeatureABC, with nested children under FeatureXYZ -->
                        <featureABCEnabled:StackPanel Margin="10,0,0,0">
                            <TextBlock Text="ABC is enabled (parent)" Foreground="Green"/>
                            
                            <!-- Nested children: only loaded if ABC is enabled AND XYZ condition met -->
                            <featureXYZEnabled:TextBlock Text="  - XYZ is also enabled (child)" Foreground="Blue" Margin="10,0,0,0"/>
                            <featureXYZDisabled:TextBlock Text="  - XYZ is disabled (child)" Foreground="Gray" Margin="10,0,0,0"/>
                        </featureABCEnabled:StackPanel>
                        
                        <featureABCDisabled:TextBlock Text="ABC is disabled" Foreground="Red" Margin="10,0,0,0"/>
                    </StackPanel>
                </Border>
				
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
