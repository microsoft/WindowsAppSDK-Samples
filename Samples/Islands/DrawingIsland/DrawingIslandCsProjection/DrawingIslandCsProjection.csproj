<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- TODO: Why do we need to specify the latest Win11 SDK instead of RS5 SDK? -->
    <TargetFramework>net8.0-windows10.0.22621.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Platforms>x64;x86;ARM64</Platforms>

    <!-- TODO: Why do we need to specify the explicit RIDs? -->   
    <RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>
  </PropertyGroup>

  <!-- See "Edit the project file to execute C#/WinRT" from https://learn.microsoft.com/en-us/windows/apps/develop/platform/csharp-winrt/net-projection-from-cppwinrt-component. -->
  <PropertyGroup>

    <!-- The CsWinRTIncludes property specifies which namespaces to project. -->
    <CsWinRTIncludes>DrawingIslandComponents</CsWinRTIncludes>

    <!-- The CsWinRTGeneratedFilesDir property sets the output directory in which the projection source files are generated. This property is set to OutDir, defined in Directory.Build.props from the section above. -->
    <CsWinRTGeneratedFilesDir>$(OutDir)</CsWinRTGeneratedFilesDir>
    <SupportedOSPlatformVersion>10.0.17763.0</SupportedOSPlatformVersion>

  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Windows.CsWinRT" />
    <PackageReference Include="Microsoft.WindowsAppSDK" />
  </ItemGroup>

  <ItemGroup>
    
    <ProjectReference Include="..\DrawingIslandComponents\DrawingIslandComponents.vcxproj">  
      <ReferenceOutputAssembly>False</ReferenceOutputAssembly>
      <BuildProject>True</BuildProject>
    </ProjectReference>
    <CsWinRTInputs Include="$(OutDir)\..\..\DrawingIslandComponents\bin\DrawingIslandComponents.winmd" />
    <!-- Native implementation DLL -->
    <None Include="$(OutDir)\..\..\DrawingIslandComponents\bin\DrawingIslandComponents.dll">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>    

  </ItemGroup>

</Project>



