<!--
//*********************************************************
//
// Copyright (c) Microsoft. All rights reserved.
//
//*********************************************************
-->
<Page
    x:Class="SDKTemplate.Scenario4_Override"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid x:Name="RootGrid" Margin="12,20,12,12">
            <ScrollViewer>
                <StackPanel>
                    <TextBlock Text="Description:" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,10,0,0">
                        Formats dates and times by overriding the user's default global context.
                        This method is used when an app presents dates or times that reflect
                        settings different from the user's current defaults. You can specify
                        overrides for language(s), region, clock, and calendar.
                    </TextBlock>
                    <Button Content="Display" Click="{x:Bind Display}" Margin="0,10,0,0"/>
                    <TextBlock x:Name="OutputTextBlock" Margin="0,10,0,0" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
